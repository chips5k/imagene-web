import React from 'react';

const ContentBodyPrimaryTopNav = (props) => {
    return (
        <div className="main__content-top-nav">
            {props.children}
        </div>
    );
}

export default ContentBodyPrimaryTopNav;

