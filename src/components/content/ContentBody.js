import React from 'react';

const ContentBody = (props) => {
    return (
        
        <div className="main__body">
            {props.children}
        </div>
      
    );
}

export default ContentBody;

